# Local secrets and runtime state — never commit.
.env
config/poller-cisco.yaml
config/poller-palo.yaml
config/discovery-cisco.yaml
config/discovery-palo.yaml
config.alloy

# Discovery runtime artifacts: written by the discovery container on each run.
state/*.yaml
!state/.gitkeep
